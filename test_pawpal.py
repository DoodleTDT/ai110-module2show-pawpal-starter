"""
This is where to test the code from pawpal_system.py and main.py
"""
from datetime import time
from pawpal_system import User, Pet, Task, Schedule


# Pytest: Verify that calling mark_complete() actually changes the task's status.


# Pytest: Verify that adding a task to a Pet increases that pet's task count.
